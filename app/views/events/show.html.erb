<div class="barreorange">
    <h1>show for event </h1>
</div>
<div class="container">
    <div class="event-info">
        <h3>
            <%= @event.title %>
        </h3>
        <h3>
            <%= @event.address %>
        </h3>
        <h3>
            <%= @event.date %>
        </h3>
    </div>
</div>
<%= link_to"back", events_path %>
<br>
<%= link_to"create a participant for this event", new_event_guest_path(@event.id) %>
